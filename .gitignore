.idea
.phpunit.cache
.phpunit.result.cache
build
composer.lock
coverage
docs
phpstan.neon
psalm.xml
testbench.yaml
vendor
node_modules
*.backup.*
test-package.sh

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
